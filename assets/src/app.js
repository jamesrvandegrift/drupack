/**
 *  Go ahead, write some es6 javascript
 */
import React from 'react';
import { render } from 'react-dom';

const RootDiv = () => (
    <div />
);

render(<RootDiv />, document.getElementById('root'));
